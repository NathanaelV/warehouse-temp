<h2>Galpões</h2>

<% @warehouses.each do |warehouse| %>
  <%= warehouse.name %>
  <%= warehouse.code %>
  <%= warehouse.city %>
  <%= warehouse.area %> m²
<% end %>


<% if @warehouses.empty? %>
  <p>Não existem galpões cadastrados.</p>
<% end %>
